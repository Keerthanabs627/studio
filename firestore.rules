rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access for this prototype.
    // In a production app, you would secure this with user authentication.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
